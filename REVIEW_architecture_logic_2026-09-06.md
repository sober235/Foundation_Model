# 架构子模块逻辑评审(v2.0)与 v2.1 处置

评审日期:2026-09-06
评审对象:`RESEARCH_PLAN.md` v2.0 §3–§6、§8 与网络架构图(Artifact 074b10a7)。
评审口径:模块之间的输入输出与真值是否自洽,不评价科学价值。

**状态更新(2026-09-06)**:用户已确认架构图解释中的 **1A / 2A / 3B / 4B**;现行定义见 `RESEARCH_PLAN.md` v2.1 与本评审 §4。下方 §0–§3 保留原评审历史口径,其中“同一个量”“必须两套解码器”的过强表述在 §4 更正,不代表现行方案。

## 0. 结论

主干逻辑成立:`patch → 实体/事件 → 关系 → 可观测性` 这条链,以及训练时"同一病例四级退化过同一网络"的回路,模块间的输入输出都对得上。问题在七处接缝,前四处需要拍板,后三处写清楚即可。

## 1. 按严重程度

### 1.1 U_Q 当成"带 3D 框的事件",与 §3.2 自相矛盾(最严重)

§3.2 把采集诱发退化定义为场状、弥散到多个结构;§4.5 的 U 解码器却是 DETR 式,每个事件出一个 3D 框并做 Hungarian 匹配。运动鬼影、噪声、欠采混叠没有"框":干预引擎只知道类型和强度,没有空间范围。由此 U_Q 的 G_ij(Δxyz、IoA)与主宿主 softmax 都没有定义。

建议:U_Q 不走事件解码器,改为全局退化 token(每类一个,像 S 一样从 F4 池化,输出类型 + 强度);它对各解剖的影响用局部保真度图定义(与 E* 同源)。

### 1.2 R(U_Q → A_i) 与 E_ij 是同一个量的两种写法

"退化影响了哪个解剖"与"这条关系在当前图上还看不看得见"都来自同一处局部损坏。现设计两条头分开算、分开定真值。

建议二选一:(a)去掉 U_Q 的关系,只保留 E;(b)两者共用局部保真度真值,并写出 E_ij 与 R(U_Q → A_i) 的函数关系。设计决定,不是画图问题。

### 1.3 退化视图上的 L_rel 与熵增项打架,方案未写明

§5.3 对 E* < τ 的关系对要求 H(R_B) ↑,而 tissue_id 真值在损坏图上仍成立。若 L_rel 也作用于损坏视图,两项互相拉扯。§8 的阶段表只在 IV 列 L_int,隐含"不对损坏视图施加 L_rel",但没有明说。

建议:写成 L_rel 在损坏视图上按 E* 加权,或只在干净视图上计算。

### 1.4 K 按域定,与"一个模型跨器官"冲突

§4.3 写膝 K = 6、脑 K ≈ 30,等于两套解剖解码器。单一模型应以所有结构的并集为 query 集合,缺席靠 no-object 表达。

建议:K = 解剖本体并集(膝 6 + 脑 SynthSeg 粒度约 30 + 后续器官),图与方案同步改。

### 1.5 关系模块缺少存在性门控

解剖解码器有 no-object,事件解码器 20 个 query 多数为空,但关系 Transformer 对 K × M 全对做自注意,主宿主 softmax 也把缺席结构算在内。fastMRI 只盖 80 mm 的卷会把大量空 token 喂进关系模块。

建议:用存在概率做注意力掩膜;主宿主 softmax 屏蔽缺席实体。

### 1.6 E 的输入与 E* 的定义域不一致

E* 定义在 Ω_ij = dilate(M_i^A ∪ B_j^U),E 的输入只有解剖侧的 F_i^local,事件侧局部特征只经 U_j 间接进入。

建议:E 头输入改为 [r_ij ; F_i^local ; F_j^local]。另注:E* 是局部 NRMSE 这种图像保真度,与"关系是否仍可见"不等价;训练用保真度、评估用模型相对失效的错位在 §5.2 有承认,图上应标出。

### 1.7 三处小缺口

- 解剖解码器出 mask 需要像素解码器把 F1–F4 融合成高分辨率逐像素特征,图上未画;只靠 stride ≥ 4 的 F2–F4,mask 太粗,算不准与 7 像素小病灶框的 IoA。
- §5.3 要求"U_Q 头预测施加的强度",U 解码器的输出只有类型和框,没有强度回归头。
- S token 既以元数据为输入,又被 L_S 监督预测对比度和场强;有元数据时预测是平凡的。要么元数据只做条件不做目标,要么训练时随机丢弃元数据。

## 2. 自洽的部分

特征层分配(事件 F1–F3、解剖 F2–F4、采集 F4)、身份锚定 query 不做匹配、关系 token 带几何与 S 条件、E 读关系 token、干预回路的三条约束与总损失四项的对应,均自洽。

## 3. 原待拍板列表(v2.0 历史)

| # | 事项 | 选项 |
|---|---|---|
| 1 | U_Q 的形态 | 全局退化 token(推荐)/ 保留框式事件但给出框的定义 |
| 2 | R(U_Q → A) 与 E 的关系 | 去掉 R_Q 只留 E(推荐)/ 同源真值 + 写出函数关系 |
| 3 | 损坏视图上的 L_rel | 按 E* 加权(推荐)/ 只在干净视图 |
| 4 | K 的定义 | 本体并集 + no-object(推荐)/ 按域 |

## 4. 用户确认与处置(v2.1,2026-09-06)

以下编号对应架构图四项及其通俗解释,与上方原评审列表编号不同。

| 用户选择 | 对应原发现 | 现行设计 |
|---|---|---|
| 1A | §1.1、§1.2、§1.7 的强度头缺口 | U_B 保留 DETR 框式检测;U_Q 从 F4 独立输出全局类型/强度,不做匹配、几何或宿主关系;移除 R_Q |
| 2A | §1.4、§1.5 | 使用固定统一本体 query 集合;区分存在、确认缺席、未知;关系注意力和主宿主候选均作存在性门控 |
| 3B | §1.3 | 干净关系监督保留;外部 detach 的 E* 对退化关系正负项、硬负项加权,低证据时关闭确定性监督,再施加不确定性目标 |
| 4B | §1.6 | E 与 E* 使用同一病灶局部配对 ROI:病灶框加其 8 mm 邻域内的宿主,裁至有效域;不再取整个器官的并集 |

**两处表述更正**:R_Q 描述退化对组织的影响,E 描述特定生物关系的局部证据,二者并不天然等价,不能直接写 E=1−R_Q;移除 R_Q 是简化输出的设计选择。按域 query 也不必然意味着两套独立模型,共享骨干加域路由同样可行;本次统一 query 是用户选择,不是“一个模型”的唯一定义。

**4B 的实现边界**:配对训练用固定、停止梯度的参考 ROI,在相同坐标和有效域上提取各视图特征并计算 E*;推理与主评估使用当前图预测的 ROI,无有效区域返回无评分,不记 E=0。报告参考/预测 ROI 差距与评分覆盖率。保真度仍是代理,不能因此宣称已验证真实关系可见性。

**尚需实现时细化**:§1.7 的高分辨率像素解码器设计与 S 元数据输入/监督策略,以及本体映射、门限和 ROI 尺度的验证,不因四项选择自动关闭。上述四项设计已定,不代表模型或实验已完成。
