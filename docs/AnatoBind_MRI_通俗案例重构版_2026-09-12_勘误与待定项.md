# 《AnatoBind-MRI 通俗案例重构版》(2026-09-12)勘误与待定项

对应文件:`docs/AnatoBind_MRI_通俗案例重构版_2026-09-12.pdf`(2026-09-12 16:15 生成,无源文件,故勘误另记于此)。它是方法与公式的讲解版;实验协议以 `RESEARCH_PLAN.md` v2.2 §13.6 为准。用户 2026-09-12 确认本文的处理意见。

## 与正在执行的方案不一致、按方案为准的三处

1. **病灶框的来源(§4、§5 结尾、§15 Step 3、§19 第 2 步、R1)。** 文档写"正式几何必须用预测的 B_j,任何地方不能用 GT 框"。2026-09-12 决定(Q19,记于 §13.6):从零训的检测头在 124 卷上不泛化(留出扫描召回 0.57、命中与落空槽位分数分不开),因此**判门路径以标注的病灶框和类别进入查表,只预测解剖**;检测降为附带报告。关系问题的前提本来就是"已经有一个异常",框是问题的输入,不是答案;宿主真值(tissue_id)从不进入几何。R1 的措辞应改为:禁止的是 GT mask、GT 宿主、GT 存在性进入几何或关系;病灶框作为查询输入是允许的,且论文中明写这是看过 fold 0 结果后的改动。
2. **病灶分支的实现(§4、§4.1)。** 文档是匿名 DETR query + Hungarian 匹配。执行中的是密集中心热图头(Q17,2026-09-11:合成实验里无参考点的 DETR 解码器 2.4 万样本后仍定不了位),且按第 1 条只作附带报告。
3. **退化类型(§12 及贯穿病例)。** 文档以运动为例贯穿。第一批只有 k 空间加噪三档与泊松欠采三档;多线圈运动仿真在 G2 不过时才做(§13.6)。

## 第二批开始前需要拍板的新设计(文档有、v2.2 没有)

| 项 | 文档(§) | v2.2 现状 | 要定的事 |
|---|---|---|---|
| 两级关系 Transformer | §7 病灶内候选竞争 + §8 跨病灶全局 | §4.6 一层,对全部有效配对自注意 | 要不要分两级;第二级是否必要(R7) |
| 主宿主 softmax 的存在软先验 h̃ = h + log p_i^A,且无 `none` 类 | §9.2 | §4.6 硬门控 + `none`(积液靠它) | `none` 必须保留;软先验是否加 |
| 病灶级可靠性 E_j(输入含 top-1/top-2 概率差),风险-覆盖选阈值 | §11 | 只有边级 E_ij | G4 的对照要改成"在置信度差之上有没有增益",否则 E_j 与 softmax 基线不独立 |
| 关系 token 含采集条件 s | §6 | 第一批已删(SKM-TEA 单协议,s 为常数) | 脑侧多协议时再加 |

## 文档指出而第一批未解决的风险

- **存在概率 p_i^A 无信息量(R2、§3.2)**:SKM-TEA 每卷六个结构都在,fold-0 试跑 presence 损失归零。需要 partial-FOV 或结构缺席的负样本,只有脑侧数据能提供;第一批不处理,报告中说明。

## 文档与执行一致、无需改动的部分

共享 3D Swin 四级特征;固定身份解剖 query 的 mask / 存在 / embedding;五维几何 G_ij;B0–B3 共用同一上游预测(R5);"失败 → 挽救 → 拒绝"三步 ≙ G2 → G3 → G4,§16.3 的核心子集 ≙ G2 的配对集合;e_ij 定义为折外任务正确性而非物理量;干净与退化图同一重建算子(R6,已实测:数据集 target 与本仓 adjoint SENSE 复数 NRMSE 1e-7)。
