# STATUS:2026-09-13（M1 第一批已完成，G2 不过；上一交接点 tag `handoff/2026-09-09`）

每次交接前整体重写本文件。五段固定:已验证、待拍板、下一步、坑与别重做、为什么。

## 1. 已完成且已验证

**裁决：G2 不过。第一批到此结束，不执行原定的运动仿真分支。**
证据与可粘贴命令在 `docs/verification/2026-09-13/G2_verdict.md`（九节，四个脚本可直接重跑）与 `docs/verification/2026-09-13/report/`。理由写进方案 §13.7。

- **G1 过**：1085 个留出的（扫描, 视图）预测一个不缺;`Upstream.forward(self, image)` 由测试钉死,推理不喂任何真值。
- **G2 不过**：判门路径（标注框和类别进查表，nnU-Net 预测解剖）下,组织族错误率 clean 0.039 → noise_q3 0.035 → us16 0.035;配对差 −0.003,95% CI 不含正值。311 个病灶、110 个扫描,功效是满的。
- **V5 §15.2 的 geometry-conflict 子集为空**：干净时对、退化后错的病灶,311 × 6 档 = **0 例**。只训干净图的分割模型 2/58,本模型（Dice 0.40）10/311。
- **三项归因**：(1) 退化几乎没伤到解剖（16× 欠采掉 2 点 Dice、移 0.46 mm）;(2) 任务对解剖质量本就不敏感（Dice 0.86→0.40 只动 4.5 个点，且 nnU-Net 掩膜比真值掩膜还好用）;(3) 门要 5–8 mm 位移，分布外分割只买到 1.02 mm。
- **余量成因**：膝侧类别限定压倒一切;脑侧无类别限定时尺度决定一切（3 mm 病灶在 1 mm 位移下 11.7% 改判，是膝侧十倍）。
- **数据侧（09-11）**：分割换成官方校正版 raw-data-track（155 个，md5 全对，Redivis `aimi.skm_tea:5r8z`）;坐标系实测为恒等（155/155）;旧 dicom-track 偏差中位 0.3–0.9 mm、最大 3.8 mm，**MTR_150 旧帧门槛失败的真因是旧分割髌骨软骨错位 3.84 mm**（推翻 09-07 结论）。新导出 `m1r`，查表天花板 0.968 → 0.958。
- **模型与训练**：上游五折（Swin + 身份锚定 A + 全分辨率 mask 头 + 密集中心热图 U_B）各 11250 步已训完并缓存预测;nnU-Net 五折 250 epoch 留出 Dice 0.841–0.862，外加只训干净图的探针（fold 5）。224 个测试通过。
- **已知的模型侧失败（09-12，非本裁决所需）**：从零训的病灶检测头在 124 卷上不泛化（留出检出近零）;mask 头过拟合（训练 0.49 / 留出 0.38），根因是数据管线（124 个整卷 + 一次翻转）而非网络，两个改进假设（热图降权、像素解码器）都被 500 步对照否定。

## 2. 待用户拍板

- **主决定**：(a) 只按测量类论文收尾;(b) 收尾 + 开新方向（小病灶 + 无类别限定 + 结构密集器官）。取决于下面第 3 条第 2 步的查证结果。
- 旧遗留（多次未答）：Q9 删除授权（SKM-TEA 2.4G truncated 残留 + 820G 原 tar）;fastMRI 其余 4850 卷是否跑 SynthSeg;是否删除已合并的旧分支。
- 提醒：对话里出现过两个 Redivis token,事后请在 https://redivis.com/workspace/settings/tokens 删除;`~/.redivis_token` 用完可删。

## 3. 下一步

1. 本次交接（已做）。
2. **查证脑侧有没有人工标注的病灶–解剖结构对应**（MS 的 McDonald 分类、转移瘤报告里的脑叶/小脑半球等）。有 → 路线五是真路;无 → 只走测量论文。约半天。
3. 按查证结果由用户定方向，再写新的 spec 与 plan。

## 4. 坑与别重做

- **别再在 SKM-TEA 膝关节上找"退化让绑定失效"**。已测死：采集退化 0.46 mm、分布外分割 1.02 mm、门要 5–8 mm;脑的模糊尺度与膝相同（同样病灶尺寸下）。
- 与 SENSE 图配对的分割一律用 raw-data-track;`m1` 只作历史，别在它上面出数。
- m1r 的 seg 头信息必须与图像层距完全一致（nnU-Net 拒收 1e-4 mm 的差）。
- 共用 GPU 上每次下发算子排队约 1.2 ms：上游 19.6 s/步、nnU-Net 1200–1500 s/epoch;空卡上 4.7 s/步、277 s/epoch。别在共用卡上估时间。
- `pkill -f <模式>` 会连自己的 shell 一起杀（退出码 144）;nnU-Net 主进程被杀后 12 个数据增广子进程会变孤儿，要按 PID `kill -9`。
- nnU-Net 的 `splits_final.json` 可以追加一个 fold 做探针（本次 fold 5），不动已完成的折;原文件备份在 `splits_final.json.bak5`。
- 轴约定、强度归一、导出续跑判据、fastMRI 手性等旧坑见 git 历史 `29142bb`。

## 5. 关键决定的为什么

- 判据从"高 10 个点"改为 G1/G2/G4（09-11）：类别感知查表干净数据上 0.968，没有 10 个点可赢。
- 判门路径改用标注框（09-12，Q19）：从零训的检测头在留出扫描上给不出框，G2 的配对集合会是空集;关系问题本就以"已有一个异常"为前提。这是看过 fold 0 结果后改的，论文里明写。
- 检测头换成密集中心热图（09-11，Q17）：DETR 解码器在合成实验里 2.4 万样本后仍定不了位（IoU 0.01），热图头 8000 样本 0.89。
- 借 GPU 0、1（09-12，Q18）：共用卡上 nnU-Net 一折 14–18 天。
- **不执行运动分支（09-13）**：V5 §17 把运动排在"前四步成立以后"，第三步失败;且门要 5–8 mm，临床到这个幅度会重扫。
