# PDF 架构说明与研究方案可行性审查

整理日期：2026-09-09。代码与方案基线：`plan-v5` 的 `bce4219`。

审查对象：[6 页架构说明 PDF](output/pdf/anatobind_architecture_explained.pdf)、[架构与 loss 总图](docs/figures/anatobind_architecture_with_losses.pdf)、[架构说明源文档](docs/architecture_and_novelty_2026-09-08.md)、[研究方案](RESEARCH_PLAN.md)。

本文件归档两轮已经完成的审查：2026-09-09 的 Codex 架构/PDF 核对，以及 2026-09-08 的 Claude Code 方案可行性审查。数值复现结果取自对应会话记录；此次归档核对了引用的代码与方案，未重新训练或复跑数据实验。审查完成不表示问题已经修复，也不表示研究目标已经验证。

## 1. 总结

| 审查对象 | 结论 | 证据边界 |
|---|---|---|
| PDF 的主干架构解释 | 基本正确，可作为架构导读；需结合下述四处限定阅读 | 当前实现是依赖 GT 辅助的膝 MRI 最小原型，完整模型与四阶段训练尚未完成 |
| 研究方案的工程可行性 | 已有数据引擎与最小训练路径，支持继续检验 | 最小路径跑通不等于证明显式关系优于对照，或证明 E 能预测关系失效 |
| M1 的科学判据 | 类别捷径与强对照使原先估计的提升空间需要重新核验 | 干净数据上的 GT 辅助 oracle 结果不能直接替代退化条件下的 Arm A 性能，尚不能据此判定 M1 必然失败 |

当前准确定位：**依赖 GT 几何的膝 MRI 结构化感知原型；完整目标是加入局部证据估计的解剖—异常关系学习框架。**

## 2. PDF 架构审查：四处限定

### 2.1 GT 辅助还包括验证裁块与评分范围

`MRI → Swin → 解剖 A / 异常 U_B → 配对关系 Transformer → R` 的主干解释正确。当前 `K=6、M=8`；`S、U_Q、E` 与完整四阶段训练仍属于研究目标。E 的设计输入是关系表示与局部图像特征，保真度代理监督不等于已校准的关系正确概率。

当前 `ArmBMinimal.forward()` 使用 GT 分割、GT 存在状态与匹配后的 GT 框构建几何/门控。验证数据还以第一个 GT 病灶框为中心裁块，宿主准确率只统计匹配且宿主已知的事件。

2026-09-09 会话核对了 fold-0 的 31 个验证扫描：完整标注中有 57 个宿主已知事件，验证裁块保留 46 个。因此，该验证口径不能代表整卷检测召回或完整绑定性能。GT 几何是已记录的最小路径取舍，报告指标时仍须显式标出。

代码入口：[dataset.py](anatobind/train/dataset.py) 的 `_origin()` / `__getitem__()`；[armb.py](anatobind/model/armb.py) 的 `ArmBMinimal.forward()`；[train_armb_minimal.py](scripts/train_armb_minimal.py)。

### 2.2 宿主 CE 清零是可复现的边界问题，历史影响尚未证明

会话复现的条件链为：某个真实宿主被候选掩膜排除 → 宿主 CE 为 `inf` → `rel_loss()` 将整批 `host_ce` 替换为零，该项不再提供梯度。其他损失项仍然存在。

在上述 46 个验证事件中没有触发该条件。可以将其记录为边界 bug，不能用它解释已有训练表现，也不能将“已复现”写成“已修复”。

代码入口：[losses.py](anatobind/model/losses.py) 的 `rel_loss()` 与 [relation.py](anatobind/model/relation.py) 的候选门控。

### 2.3 位置编码是待验证风险，不能推出模型无法定位

解码器没有显式坐标输入。原会话重排解码器输入的 memory token 后，预测框变化约为 `6 × 10⁻⁸`，支持“该解码器对 memory token 排列不敏感”的判断。

骨干特征可能已经携带位置线索。上述试验既不能推出原始 MRI 空间打乱后预测不变，也不能证明完整模型无法定位。是否增加位置编码，应由完整模型的定位实验与消融决定。

代码入口：[decoders.py](anatobind/model/decoders.py)、[backbone.py](anatobind/model/backbone.py)。

### 2.4 输出形式不等于已经学会的能力

独立 sigmoid 可以表达多个关联概率，但当前每个事件仅以一个 `host_label` 作为正例监督。`none` 有输出槽位，当前最小训练路径却没有对应的正例训练。

`none` 表示“事件确无宿主”，不表示“看不清所以弃权”。未知宿主、无有效 ROI 与低证据也不能直接改写成 `none`。E 的保真度监督仍是代理，不能仅由输出范围推断其概率校准或失效预测能力。

代码入口：[losses.py](anatobind/model/losses.py) 的 `build_relation_targets()`、[dataset.py](anatobind/train/dataset.py) 的 `read_in_seg_boxes()`；完整定义见 [研究方案](RESEARCH_PLAN.md) §4–§5。

## 3. 方案可行性审查：类别捷径与 M1

### 3.1 历史测量及统计口径

D5 的类别—宿主组织约束，使半月板撕裂对应半月板，软骨病变对应软骨候选集合。只比较不使用类别约束的重叠绑定器，会低估合理对照的能力。

2026-09-08 可行性审查在 155 个扫描、309 个宿主可评分实例上报告了以下探索性测量。309 是 311 个分割内实例去除 2 个宿主未解析实例后的口径；半月板撕裂 101 个，软骨病变 208 个。

| 方法 | 组织族级 ABA | 条件 |
|---|---:|---|
| argmax-IoA，无病灶类别约束 | 0.864 | 完整 GT 框、GT 分割，309 个实例 |
| argmax-IoA，加病灶类别约束 | 0.958 | 同一 GT 辅助口径，309 个实例 |
| 病灶类别 + 框坐标，逻辑回归 | 0.799 | 按 scan 五折的探索性结果；不读取图像 |
| 仅框坐标，逻辑回归 | 0.599 | 按 scan 五折的探索性结果；不读取图像 |

无类别约束时有 42 个组织族判定错误；加入约束后剩余 13 个，均为软骨病变。类别加框坐标的探索性分类中，半月板撕裂子集为 1.000，软骨子集为 0.702。类别约束重叠绑定器的标签级 ABA 为 0.958，侧别解析子集（144 个实例）为 0.979。

这些数字不能混用：方案 §13.4 的 `0.860` 来自 fold-0 完整框的 57 个事件；此处 `0.864` / `0.958` 来自全库 309 个可评分实例。已核对原逻辑回归脚本 `noanat.py`：它按 `splits.json` 的 scan 分组执行五折。原始数据清单与逐例预测未在本次归档中复核；这些结果仍应作为 GT 类别/框条件下的探索性对照，不作为图像模型的部署性能。

代码与口径入口：[skmtea.py](anatobind/data_engine/skmtea.py) 的 `SUPER_TO_TISSUES`、[研究方案](RESEARCH_PLAN.md) §5.1、§9.1、§13.4。

### 3.2 对原可行性审查中强表述的限定

**`0.958` 是一个使用真值输入的重叠算法结果，不是训练后 Arm A 的实测性能，也不是任何关系算法都无法超过的数学上界。** 部署评估需要预测的病灶类别、框和分割，须计入这些上游误差。

`0.958 + 0.10 = 1.058` 只说明：若要求在这一相同的干净 GT 辅助口径下再提升 10 个百分点，则不可能满足。现行 M1 明确针对退化条件，故该算式不能证明原 M1 必然不可行。实际可行性需要两臂在同一退化条件与预测输入下的比较。

原审查中的“软骨准确率需降到约 0.79”也是条件推演。若半月板子集在两臂间几乎无差异，则总体提升 0.10 约要求软骨子集提升 `0.10 / (208 / 309) ≈ 0.149`。只有进一步假设 Arm B 的软骨准确率约为 0.938，才得到 Arm A 需低于约 0.79；这不能直接作为新的停止阈值。

原会话还将约 12M 的计划骨干与当前最小模型一并讨论。应区分设计与实现：[架构说明](docs/architecture_and_novelty_2026-09-08.md) 记录当前最小模型共 3,949,137 参数、骨干 1,758,212 参数。两臂公平性需要按实际训练配置核对，不能把计划规模当成已运行配置。

### 3.3 尚待实验回答的问题

- 退化条件下，类别约束的 Arm A 在软骨子集上实际下降多少；Arm B 在相同上游预测、监督与统计口径下能否改善。
- E 的目标事件是否足够：若宿主主要由类别决定，关系失效过少，则不能仅靠 AURC/ECE 声称已验证失效预测。
- 侧别标签部分由重叠规则解析；若将侧别作为主要空间推理终点，需要独立真值核验。
- 审查时的导出含噪声与欠采样，尚无 motion；完整退化结论需要补齐相应数据与验证。
- 最小路径的 GT 辅助成绩不能替代预测几何下的结果；应分别报告分割、检测与绑定误差。

## 4. 建议的后续顺序

以下为审查建议，尚未替代 [研究方案](RESEARCH_PLAN.md) 中已经确认的判据与设计决策。

1. 固定完整卷/裁块、GT/预测输入、组织族/标签级、已知/未知宿主等统计口径，保存逐例预测与评分覆盖率。
2. 修复并验证宿主 CE 非有限值处理；将该边界问题与尚未证实的历史训练影响分开记录。
3. 先获得包含类别约束的 Arm A 退化曲线，同时报告整体与软骨子集，并使两臂使用可比较的上游输入与监督。
4. 用开发集或预先规定的流程评估判据调整需求；不要依据最终测试结果事后选择子集或阈值。
5. 补 motion、预测 ROI/几何评估及必要的独立侧别核验，再评估局部证据 E 与真实关系失效的联系。

保留的研究价值是显式异常—解剖关系、可控 k-space 干预和局部证据监督的组合。上述审查支持继续检验这些命题，尚不支持“已验证的 foundation model”“已证明临床可靠性”或“显式关系必然优于合理对照”等结果性表述。

## 5. 归档说明

两轮审查分别于 2026-09-08 22:22 和 2026-09-09 12:57（北京时间）给出结论。Claude Code 会话在本次状态检查时仍存活但处于 `idle`；其后续是否恢复执行不由本文跟踪。

本次归档新增本报告，并在架构说明源文档中加入入口。原 PDF 随分支保留；阅读时应结合本报告的限定。没有修改模型、研究判据或 PDF，也没有将这些建议标记为已修复或已批准。
